# SAGE User Profile

以下是根据对话材料生成的用户画像：

# SAGE User Profile

## Inferred identity and work context
- 身份：可能是地球科学、地质学、海洋学或相关领域的研究人员或学生。
- 工作场景：需要快速生成标准化的地理底图（如中国地形图）用于报告、论文或教学。

## Research/software interests
- 核心软件：**GMT (Generic Mapping Tools)** — 用户重复请求同一绘图任务，证明其日常依赖 GMT 进行地理数据可视化。
- 兴趣领域：区域地形绘图、底图制作，可能涉及地球物理或环境科学。

## Habits and preferences
- 交互方式：直接下达具体指令（“帮我用GMT绘制…”），无需额外解释或参数调整，依赖助手默认配置。
- 任务重复性：相同请求出现两次，暗示可能频繁使用相同命令（如固定分辨率 `02m_relief`），或测试系统一致性。
- 文件偏好：接受 PNG 格式输出，文件大小约 3MB，分辨率适中。

## Knowledge level
- GMT 基础操作熟练：知道如何调用默认网格和绘图命令，但不一定深入定制（如修改色标、投影）。
- 可能不熟悉高级参数或调优，但了解标准工作流（下载数据 → 生成图件）。

## Intent and routing hints
- 常见意图：**快速生成标准区域底图**，不需要解释或中间步骤。
- 路由偏好：直接路由到 `gmt_plotting` 技能，输出图像与下载链接即可，无需附加理论说明。
- 后续助手操作：应直接执行 GMT 命令（使用 `earth_relief_02m` 默认设置），返回图片与简短确认。

## Open uncertainties
- 用户是否也使用其他绘图工具（如 matplotlib、PyGMT）？当前对话无证据。
- 用户是否对特定区域、投影、色标有隐藏偏好的默认值？需要更多交互推断。
- 用户是否希望获得可复用的脚本（如 `.sh` 文件）？此前对话自动生成了 `analysis.sh`，但用户未明确要求。

---
Updated: 2026-05-07T23:28:01
